<block wx:if="{{$taroCompReady}}">
    <at-tabs-pane compid="{{$compid__127}}">
        <block>
            <block wx:if="{{anonymousState__temp2}}">
                <block>
                    <block wx:if="{{list.length}}">
                        <view class="productList">
                            <product-item wx:key="code" wx:for="{{loopArray17}}" wx:for-item="product" wx:for-index="_anonIdx2" compid="{{product.$compid__126}}"></product-item>
                        </view>
                    </block>
                    <block wx:else>
                        <view class="product-empty">
                            <view class="view-image">
                                <image class="image" mode="widthFix" lazy-load="{{true}}" src="{{anonymousState__temp}}"></image>
                            </view>
                            <view class="tips">产品维护中，如需充值请联系客服</view>
                        </view>
                    </block>
                </block>
            </block>
            <block wx:else>
                <block wx:if="{{anonymousState__temp3}}">
                    <view class="initTips">{{initTips}}</view>
                </block>
            </block>
        </block>
        <block wx:if="{{anonymousState__temp4}}">
            <view class="flowProductDetail">
                <view class="flowProductDetail-info">
                    <view class="title">{{currentProduct.name}}
                        <block wx:if="{{currentProduct.tips}}"><text class="tips">{{currentProduct.tips}}</text>
                        </block>
                    </view><text class="info">{{currentProduct.info}}</text>
                </view>
                <view class="flowProductDetail-buy">
                    <view class="price">￥{{currentProduct.price}}</view>
                    <view class="button" bindtap="anonymousFunc0">购买</view>
                </view>
            </view>
        </block>
    </at-tabs-pane>
</block>