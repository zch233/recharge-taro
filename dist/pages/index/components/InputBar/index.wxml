<block wx:if="{{$taroCompReady}}">
    <view class="InputBar">
        <view class="InputBar-top">
            <view class="view-image">
                <image class="image" bindtap="anonymousFunc0" mode="widthFix" src="https://oss.globalcharge.cn/prod/wechat/countryFlags/China.svg"></image>
            </view>
            <view class="phoneInputBar">
                <view class="areaNumber">+{{state.currentCountry.areaNumber}}</view>
                <view class="phoneInput">
                    <at-input compid="{{$compid__66}}"></at-input>
                </view>
            </view>
        </view>
        <view class="{{(state.phoneInputHighLight && 'active') + ' phoneInputBg'}}"></view>
        <view class="InputBar-bottom">
            <view class="countryName">{{state.currentCountry.cname}}</view>
            <view class="carrierName">{{state.carrierName}}</view>
        </view>
        <country-list compid="{{$compid__67}}"></country-list>
    </view>
</block>