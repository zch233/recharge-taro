<block wx:if="{{$taroCompReady}}">
    <view>
        <over-header></over-header>
        <input-bar compid="{{$compid__32}}"></input-bar>
        <product-bar compid="{{$compid__33}}"></product-bar>
        <button bindtap="anonymousFunc0">测试专用，误点。</button>
        <navigator class="bottomOrderList" url="/pages/record/record">充值记录</navigator>
    </view>
</block>