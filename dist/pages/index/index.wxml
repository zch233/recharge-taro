<block wx:if="{{$taroCompReady}}">
    <view>
        <over-header></over-header>
        <input-bar compid="{{$compid__27}}"></input-bar>
        <product-bar compid="{{$compid__28}}"></product-bar>
    </view>
</block>