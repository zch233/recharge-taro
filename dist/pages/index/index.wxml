<block wx:if="{{$taroCompReady}}">
    <view>
        <over-header></over-header>
        <input-bar compid="{{$compid__122}}"></input-bar>
        <product-bar compid="{{$compid__123}}"></product-bar>
    </view>
</block>